# Save Format

## 方針

セーブデータは、アップデート後も可能な限り古いデータを読み込めることを重視する。

静的なゲーム定義と、プレイヤーの進行データを分離する。

- 鉱石・機械・アップグレード定義: Godot Resourceなどの静的データ
- プレイヤー進行: セーブファイル

## MVP形式

MVPではJSONベースを第一候補とする。

理由:

- 内容を確認しやすい
- デバッグしやすい
- バージョン移行処理を書きやすい
- 小規模なシングルプレイデータとして十分

## 必須フィールド

概念例:

```json
{
  "save_version": 1,
  "money": 0,
  "inventory": {},
  "upgrades": {},
  "player": {
    "position": [0, 0, 0]
  },
  "machines": []
}
```

これは現時点の概念例であり、実装開始時に正式スキーマを確定する。

## バージョニング

必ず `save_version` を持つ。

読み込み時:

1. 現在のバージョンと一致 → 通常ロード
2. 古いバージョン → マイグレーション
3. 未知の新しいバージョン → 安全に失敗させる

## 保存先

Godotの `user://` 配下を使用する。

Repository内に実プレイヤーのセーブデータをコミットしない。

## MVPで保存するもの

- money
- inventory
- upgrades
- player position
- placed machines
- machine inventories

## 将来候補

- バックアップセーブ
- 書き込み中断対策
- セーブスロット
- オフライン進行用timestamp
- 統計
- 実績
